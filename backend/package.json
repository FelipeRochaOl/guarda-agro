{
  "name": "guarda-agro-backend",
  "version": "1.0.0",
  "description": "Backend do GuardaAgro — Análise de Risco Ambiental com dados da NASA",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir ./dist --target bun"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.2.0",
    "elysia": "^1.2.25",
    "firebase-admin": "^13.10.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.3"
  }
}
